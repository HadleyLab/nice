# Git Automation Rules
1. Make documentation updates first
2. Commit all changes with message format: "MB: [brief change description]"
3. Include all Memory Bank files in every commit
4. Commit must follow documentation updates (never precede them)
